<div class="text-center">
  <h2>{{city}}</h2>
  <p>Population: {{population}}</p>
  <p>Lanmarks: {{landmarks}}</p>
  <p>Entertainment: {{entertainment}}</p>
  <p>Excursion Type: {{excursion_type}}</p>
  <p>Travel Season: {{travel_time}}</p>
  <img class="someClass" src="{{img_url}}">
  {{!-- <p>Created by {{user.name}} on {{format_date user.createdAt}}</p> --}}
  {{!-- <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p> --}}
  <p>
    <button class="btn btn-primary">Fund this City!</button>
  </p>
</div>
